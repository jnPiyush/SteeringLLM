# Documentation Workflow — Docs-only changes
# Engineer writes with iterative refinement for completeness

[workflow]
name = "docs"
description = "Documentation-only changes -- engineer with iterative refinement"
type = "docs"

[[steps]]
id = "write"
title = "Write documentation: {{doc_title}}"
agent = "engineer"
iterate = true
max_iterations = 5
completion_criteria = "All sections complete, no broken links, spell-checked, examples tested"
status_on_start = "In Progress"
status_on_complete = "Done"
